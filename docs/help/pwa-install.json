{
  "id": "mq4ve3vs8ivo",
  "title": "Install as Local App (PWA)",
  "content": "<h1>Install as Local App (PWA)</h1><p><strong>Meshbex CAD Explorer</strong> supports installation as a <strong>Progressive Web App (PWA)</strong>. Once installed, the application is cached locally in your browser and can be launched and used <strong>without an internet connection</strong>.</p><hr><h2>Requirements</h2><ul><li><p>A <strong>Chromium-based browser</strong> – Google Chrome, Microsoft Edge, or Brave. Firefox does not support PWA installation.</p></li><li><p>An <strong>internet connection</strong> for the first installation. After that, no internet is needed.</p></li></ul><hr><h2>How to Install</h2><ol><li><p>Open your browser and navigate to <code>https://meshbex.com</code>.</p></li><li><p>Wait for the page to fully load.</p></li><li><p>Look for the <strong>install icon</strong> in the browser address bar on the right side – it looks like a computer with a download arrow (⊕).</p></li><li><p>Click it and confirm by clicking <strong>Install</strong>.</p></li><li><p>A shortcut will appear on your <strong>desktop</strong> and/or in the Start menu.</p></li></ol><p>Alternatively, use the browser menu:</p><ul><li><p><strong>Chrome</strong> – menu (⋮) → <em>Save and share</em> → <em>Install as app…</em></p></li><li><p><strong>Edge</strong> – menu (⋯) → <em>Apps</em> → <em>Install this site as an app</em></p></li></ul><hr><h2>Launching After Installation</h2><p>Double-click the <strong>desktop shortcut</strong>. The application opens in its own standalone window – without the browser address bar – and works fully offline.</p><hr><h2>How Offline Mode Works</h2><p>On the first visit, the browser downloads and caches the entire application (JavaScript, CSS, icons, demo models, help files). All subsequent launches load from this local cache, so <strong>no internet connection is required</strong>.</p><p>When you are online, the cache is silently updated in the background so you always get the latest version.</p><hr><h2>Sharing the App with Another Computer</h2><p>The PWA cache cannot be copied between computers. To install on another machine:</p><ol><li><p>Connect that computer to the internet (even briefly).</p></li><li><p>Open <code>https://meshbex.com</code> in Chrome or Edge.</p></li><li><p>Click the install icon and install.</p></li><li><p>The computer can then be used offline.</p></li></ol><hr><h2>Uninstalling</h2><ul><li><p>Open the app, click the menu (⋮ / ⋯) → <strong>Uninstall</strong>.</p></li></ul><hr><h2>Notes</h2><ul><li><p>Imported models (loaded via <em>Import GLB</em>) are kept in memory for the current session only – they are not cached permanently.</p></li><li><p>Clearing browser data or cache will remove the installed PWA cache. You will need to be online for the next launch to re-cache.</p></li><li><p>The PWA remains installed indefinitely as long as it is used occasionally. Browsers may evict unused PWA caches after several weeks of inactivity.</p></li></ul><p></p>",
  "font": "Arial, sans-serif",
  "lineHeight": "1.4",
  "paraSpacing": "8px",
  "docWidth": "794px",
  "tableBorder": "1px solid #ccc",
  "originalId": "mq4va8ifzz14",
  "importedAt": "2026-06-08T07:09:01.864Z",
  "lastEditAt": "2026-06-08T07:09:23.755Z"
}